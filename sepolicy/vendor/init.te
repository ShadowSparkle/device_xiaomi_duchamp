# Charge Bypass
allow init sysfs_battery_supply:file rw_file_perms;

rw_dir_file(init, sysfs_ged)
rw_dir_file(init, sysfs_gpu)

# Turbo Charge
allow init sysfs_power_supply:file { setattr };
r_dir_file(init, sysfs_batteryinfo)
